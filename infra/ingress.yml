version: "3"
services:
  ingress:
    image: nginx
    restart: always
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
    - 80:80
    command: [ nginx-debug, '-g', 'daemon off;' ]
    networks:
      - dc-net
networks:
  dc-net:
    driver: bridge
    name: dc-net